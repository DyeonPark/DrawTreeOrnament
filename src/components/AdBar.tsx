import './AdBar.css';

const AdBar = () => {
    return (
        <div className="ad-bar-container">
            <div className="ad-placeholder">
                ADVERTISEMENT AREA (Google AdSense / Kakao AdFit)
            </div>
        </div>
    );
};

export default AdBar;
